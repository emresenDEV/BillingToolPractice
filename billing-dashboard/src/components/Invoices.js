import React from "react";

const Invoices = () => {
return (
<div>
    <h1>Invoices</h1>
    <p>Create or edit invoices here.</p>
    {/* Add a form to create or update invoice details */}
</div>
);
};

export default Invoices;
